import { View } from 'react-native';

// TODO(subagent-3): replace this placeholder with the Library screen
// (BrandMark + Wordmark header, "no decks yet" empty state, FAB "New deck"
// pinned bottom-right). Owned by GitHub issue #1, Phase 3.
export default function LibraryScreen() {
  return <View className="flex-1 bg-background" />;
}
